#include "main.h"
#include <stdio.h>
#include <ctype.h>

/**
 *_abs - Prints absolute values
 *
 *@n: stores value from absolut number
 *Return: n value
 */
int _abs(int n)
{
	if (n < 0)
	{
	n = (n * (-1));
	}


return (n);
}
